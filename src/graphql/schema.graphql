# comentario sencillo
type Query {
    hello: String!
    product(id: ID!): Product
    products: [Product!]!
}

"""
Comentario multiple
"""
type Product {
    id: ID!
    name: String!
    price: Float!
    description: String!
    image: String!
    createdAt: String!
    options: String!
}
